using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelExistence : MonoBehaviour
{
    void Start()
    {
        // We are using it to know that we are in Level 1. If we are in level 1 we are destroying the Audio from Previous Scenes *Happy Face*
    }
}
